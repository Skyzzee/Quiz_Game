{% extends 'base.html.twig' %}

{% block title %}Ajouter un Thème{% endblock %}

{% block body %}
    <h1>Ajouter un Thème</h1>

    {{ include('category/_form.html.twig') }}

    <a href="{{ path('app_category_index') }}" class="btn btn-primary">Retour à la liste de thème</a>
    <a href="{{ path('app_exercise_index') }}" class="btn btn-primary">Retour à la création de quiz</a>
{% endblock %}