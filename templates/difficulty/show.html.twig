{% extends 'base.html.twig' %}

{% block title %}Création de difficulté{% endblock %}

{% block body %}
    <h1>Création de nouvelle difficulté</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ difficulty.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ difficulty.name }}</td>
            </tr>
            <tr>
                <th>Timer</th>
                <td>{{ difficulty.timer }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_difficulty_index') }}" class="btn btn-primary">Retour à la liste de difficulté</a>
    <a href="{{ path('app_exercise_index') }}" class="btn btn-primary">Retour à la création de quiz</a>

    <a href="{{ path('app_difficulty_edit', {'id': difficulty.id}) }}">Modifier</a>

    {{ include('difficulty/_delete_form.html.twig') }}
{% endblock %}
